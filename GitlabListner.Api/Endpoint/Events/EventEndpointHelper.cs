namespace GitlabListener.Endpoint.Events;

public static class EventEndpointHelper
{
    //public static RouteGroupBuilde.r EventEndpoint(this RouteGroupBuilder builder)
    //{
    //    builder.MapGet("/", static () => Results.Ok("This endpoint handles webhook trigger events for different events such as issues, push , etc"));
    //    builder.MapPost("/issues/", (IssueEventListener handler, IssuesEvent issuesEvent) => handler.Handler(issuesEvent));
    //    return builder;
    //}
}